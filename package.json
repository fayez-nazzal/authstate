{
  "name": "authstate",
  "version": "0.2.1",
  "description": "Log in once and maintain a shared Playwright storageState cookie jar for browser tools.",
  "type": "module",
  "bin": {
    "authstate": "src/main.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
